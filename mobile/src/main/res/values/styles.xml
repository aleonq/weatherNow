<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ThemeTextView" parent="android:Widget.TextView">
        <item name="android:layout_margin">@dimen/margin_std_internal</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="ThemeTextBrief" parent="ThemeTextView">
        <item name="android:textSize">@dimen/textsize_desc</item>
    </style>

    <style name="ThemeTextDetail" parent="ThemeTextView">
        <item name="android:layout_margin">@dimen/margin_std_internal</item>
        <item name="android:textSize">@dimen/textsize_details</item>
        <item name="android:gravity">left|start</item>
    </style>
</resources>